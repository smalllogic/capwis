  <nav class="bg-white/95 backdrop-blur-md border-b border-slate-200 fixed w-full z-30 top-0 start-0" data-controller="navbar">
  <div class="max-w-screen-2xl flex flex-wrap items-center justify-between mx-auto px-4 md:px-8 py-4">
    <div class="flex items-center">
      <a href="<%= root_path %>" class="md:hidden flex items-center mr-2">
        <% if @site_config.logo.attached? %>
          <%= image_tag @site_config.logo, class: "h-6 w-auto object-contain", alt: @site_config.name %>
        <% else %>
          <%= image_tag "hatlogo.webp", class: "h-6 w-auto object-contain", alt: "Lincaps Logo" %>
        <% end %>
      </a>
      <a href="<%= root_path %>" class="hidden md:flex items-center space-x-3 rtl:space-x-reverse mr-4 md:mr-12 flex-shrink-0">
        <% if @site_config.logo.attached? %>
          <%= image_tag @site_config.logo, class: "h-8 sm:h-10 md:h-12 lg:h-14 w-auto max-w-[120px] sm:max-w-[150px] md:max-w-[200px] lg:max-w-[240px] object-contain transition-all duration-300", alt: @site_config.name %>
        <% else %>
          <%= image_tag "hatlogo.webp", class: "h-8 sm:h-10 md:h-12 lg:h-14 w-auto max-w-[120px] sm:max-w-[150px] md:max-w-[200px] lg:max-w-[240px] object-contain transition-all duration-300", alt: "Lincaps Logo" %>
        <% end %>
      </a>
    </div>
    <div class="flex md:order-2 space-x-3 items-center">

      <div class="hidden md:flex">
        <% if user_signed_in? %>
          <div class="relative group/user">
            <button type="button" class="flex items-center space-x-2 text-sm font-medium text-slate-700 hover:text-primary transition-all bg-slate-50 p-1.5 rounded-full border border-slate-200 pr-3">
              <div class="w-8 h-8 rounded-full bg-gradient-to-br from-primary to-dark flex items-center justify-center text-white text-xs font-black shadow-lg shadow-primary/20">
                <%= current_user.email[0..1].upcase %>
              </div>
              <svg class="w-4 h-4 text-slate-400 group-hover/user:rotate-180 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
            </button>

            <!-- User Dropdown -->
            <div class="absolute right-0 top-full pt-2 hidden group-hover/user:block w-48 z-50">
              <div class="bg-white border border-slate-200 rounded-2xl shadow-2xl overflow-hidden backdrop-blur-xl bg-opacity-95 py-2 ring-1 ring-slate-900/5 animate-fade-in-up">
                <div class="px-4 py-2 border-b border-slate-100 mb-2">
                  <p class="text-[10px] text-slate-400 font-bold uppercase tracking-widest"><%= t('navbar.signed_in_as', default: 'Signed in as') %></p>
                  <p class="text-xs text-slate-900 font-bold truncate"><%= current_user.email %></p>
                </div>
                
                <% if current_user.admin_role? %>
                  <%= link_to admin_root_path, class: "flex items-center px-4 py-2 text-sm text-primary hover:bg-slate-50 transition-colors font-bold" do %>
                    <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path></svg>
                    <%= t('navbar.admin_dashboard', default: 'Admin Panel') %>
                  <% end %>
                <% end %>

                <%= button_to destroy_user_session_path, method: :delete, class: "flex items-center w-full px-4 py-2 text-sm text-red-600 hover:bg-slate-50 transition-colors font-bold text-left" do %>
                  <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path></svg>
                  <%= t('navbar.logout') %>
                <% end %>
              </div>
            </div>
          </div>
        <% else %>
          <div class="flex items-center space-x-1.5">
            <%= link_to new_user_session_path, class: "group relative p-1.5 text-slate-600 hover:text-slate-900 bg-slate-50 rounded-full border border-slate-200 transition-all hover:shadow-md", title: t('navbar.login') do %>
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1"></path>
              </svg>
              <span class="absolute -bottom-8 left-1/2 -translate-x-1/2 px-2 py-1 bg-slate-800 text-white text-[10px] rounded opacity-0 group-hover:opacity-100 transition-opacity whitespace-nowrap pointer-events-none z-50">
                <%= t('navbar.login') %>
              </span>
            <% end %>
          </div>
        <% end %>
      </div>

      <button data-action="click->navbar#toggle" type="button" class="inline-flex items-center p-2 w-10 h-10 justify-center text-sm text-slate-500 rounded-lg md:hidden hover:bg-slate-100 focus:outline-none focus:ring-2 focus:ring-slate-200" aria-controls="navbar-sticky" aria-expanded="false">
        <span class="sr-only">Open menu</span>
        <svg class="w-5 h-5" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 17 14">
          <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 1h15M1 7h15M1 13h15"/>
        </svg>
      </button>
    </div>

    <div class="items-center justify-between hidden w-full md:flex md:w-auto md:order-1 flex-grow" id="navbar-sticky" data-navbar-target="menu">
      <ul class="flex flex-col p-4 md:p-0 mt-4 font-medium border border-slate-100 rounded-lg bg-white md:space-x-4 lg:space-x-6 rtl:space-x-reverse md:flex-row md:mt-0 md:border-0 md:bg-transparent justify-center text-xs lg:text-sm">
        <li>
          <%= link_to t('navbar.home'), root_path, class: "block py-2 px-3 #{current_page?(root_path) ? 'text-primary font-bold border-b-2 border-primary' : 'text-slate-600 hover:text-primary'} transition-all duration-300 rounded-sm md:bg-transparent md:p-0" %>
        </li>
        <li>
          <%= link_to t('navbar.about_us'), about_path, class: "block py-2 px-3 #{current_page?(about_path) ? 'text-primary font-bold border-b-2 border-primary' : 'text-slate-600 hover:text-primary'} transition-all duration-300 rounded-sm md:bg-transparent md:p-0" %>
        </li>
        <li>
          <%= link_to t('navbar.factory'), factory_path, class: "block py-2 px-3 #{current_page?(factory_path) ? 'text-primary font-bold border-b-2 border-primary' : 'text-slate-600 hover:text-primary'} transition-all duration-300 rounded-sm md:bg-transparent md:p-0" %>
        </li>

        <% [ 'a', 'b', 'c' ].each do |kind| %>
          <% categories_for_kind(kind).each do |category| %>
            <li class="relative group/cat-0" data-navbar-target="rootDropdown">
              <div class="flex items-center">
                <%= link_to category.localized_name, category_path(category), class: "block py-2 px-3 #{params[:id].to_i == category.id ? 'text-primary font-bold border-b-2 border-primary' : 'text-slate-600 hover:text-primary'} transition-all duration-300 rounded-sm md:bg-transparent md:p-0 flex-grow" %>
                <% if category.children.visible.any? %>
                  <button type="button" data-action="click->navbar#toggleMobileRoot" class="md:hidden ml-auto text-slate-400 p-2 focus:outline-none">
                    <svg class="w-4 h-4 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
                  </button>
                <% end %>
              </div>
              
              <% if category.children.visible.any? %>
                <!-- Desktop/Mobile Dropdown -->
                <div class="hidden md:group-hover/cat-0:block md:absolute md:left-0 md:top-full w-full md:w-64 z-50 pt-2 dropdown-container pointer-events-auto">
                  <div class="bg-white border border-slate-200 rounded-2xl shadow-2xl overflow-visible backdrop-blur-xl bg-opacity-95 py-2 ring-1 ring-slate-900/5 md:animate-fade-in-up">
                    <% category.children.visible.each do |child| %>
                      <%= render 'layouts/navbar_category', category: child, kind: kind, depth: 1 %>
                    <% end %>
                  </div>
                </div>
              <% end %>
            </li>
          <% end %>
        <% end %>

        <li>
          <%= link_to "Blog", posts_path, class: "block py-2 px-3 #{current_page?(posts_path) ? 'text-primary font-bold border-b-2 border-primary' : 'text-slate-600 hover:text-primary'} transition-all duration-300 rounded-sm md:bg-transparent md:p-0" %>
        </li>
        <li>
          <%= link_to t('navbar.contact_us'), contact_path, class: "block py-2 px-3 #{current_page?(contact_path) ? 'text-primary font-bold border-b-2 border-primary' : 'text-slate-600 hover:text-primary'} transition-all duration-300 rounded-sm md:bg-transparent md:p-0" %>
        </li>
        <li class="md:hidden pt-4 border-t border-slate-100 mt-2">
          <% if user_signed_in? %>
            <div class="flex flex-col space-y-3 px-3">
              <div class="flex items-center space-x-3 mb-2">
                <div class="w-10 h-10 rounded-full bg-gradient-to-br from-slate-700 to-slate-900 flex items-center justify-center text-white text-xs font-black shadow-lg">
                  <%= current_user.email[0..1].upcase %>
                </div>
                <div>
                  <p class="text-[10px] text-slate-400 font-bold uppercase tracking-widest leading-none mb-1"><%= t('navbar.signed_in_as', default: 'Signed in as') %></p>
                  <p class="text-xs text-slate-900 font-bold truncate"><%= current_user.email %></p>
                </div>
              </div>
              
              <div class="flex items-center space-x-4 pt-2">
                <% if current_user.admin_role? %>
                  <%= link_to t('navbar.admin_dashboard', default: 'Admin'), admin_root_path, class: "text-primary font-bold text-sm" %>
                  <span class="text-slate-300">|</span>
                <% end %>
                <%= button_to t('navbar.logout'), destroy_user_session_path, method: :delete, class: "text-red-600 font-bold text-sm" %>
              </div>
            </div>
          <% else %>
            <div class="flex flex-col space-y-4 px-3">
              <div class="flex flex-col space-y-1.5">
                <%= link_to t('navbar.login'), new_user_session_path, class: "text-slate-600 hover:text-slate-900 py-1.5 text-xs font-medium border-b border-slate-100" %>
              </div>
            </div>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>
